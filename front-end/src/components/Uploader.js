import { useState } from 'react'

function Uploader() {
    return(
        <main>
            <form action="">
                <input type="text" accept='image/*'/>
            </form>
        </main>
    )
}

export default Uploader